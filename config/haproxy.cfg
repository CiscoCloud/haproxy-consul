template {
  source = "/consul-template/template.d/haproxy.tmpl"
  destination = "/haproxy/haproxy.cfg"
  command = "/usr/sbin/haproxy -db -p /var/run/haproxy.pid  -f /haproxy/haproxy.cfg -sf || true"
}
